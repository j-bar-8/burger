<li>
    {{name}}

    <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
        {{#if devour}}Eat It!{{else}}Make Another One{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Remove</button>
</li>